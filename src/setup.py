
from setuptools import setup

setup(
   name="PLS",
   version='1.0',
   description='Home functions for Part 2 Project',
   author='Ollie Donaldson',
   author_email='odb28@cam.ac.uk',
   packages=["PLS"],  #same as name
   install_requires=['numpy'], #external packages as dependencies
)
